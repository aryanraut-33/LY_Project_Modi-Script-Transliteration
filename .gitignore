# --- Python Environment & Cache ---
venv/
env/
.venv/
__pycache__/
*.py[cod]
*$py.class

# --- macOS Specific ---
.DS_Store
.AppleDouble
.LSOverride

# --- IDEs ---
.vscode/
.idea/

# --- Project Outputs (Optional but Recommended) ---
# We usually ignore the trained models because they are 200MB+ each
# even if the raw data is small.
outputs/models/*.h5
outputs/logs/*.csv
outputs/logs/*.png

# --- Data ---
#ignore the processed data to save space,as it can be regenerated by running the script.
data/processed/